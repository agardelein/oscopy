To be checked :
ioscopy user manual screen captures
warn user about usage of drag and drop for multiple signals selected from the main window (on click the selection disappear but all previously selected signals are dropped in the Figure)

To be fixed :
ui: destroy figure
cursors: line not on all graph after unzoom
support for command 'orange reset' reseting the graph range

To be added :
Support for HDF5
Make Signal derive from numpy.array
Support for SpanSelector
Support for Signal compatible diff()erenciacion
Support for other graph format: Smith charts, polar plots,...
Support for other languages (currently only en, es)
Separate the gui from the framework: make two packages oscopy-core and oscopy-gtkgui
